export class ThumbAnnouncement {
  constructor(
    id,
    title,
    cover,
    pictures,
    description,
    host,
    rating,
    location,
    equipments,
    tags
  ) {
    this.id = id
    this.title = title
    this.cover = cover
    this.pictures = pictures
    this.description = description
    this.rating = rating
    this.location = location
    this.equipments = equipments
    this.tags = tags
  }
}
